"use strict";
/*
 * Copyright (c) 2025 Huawei Device Co., Ltd.
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *     http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || function (mod) {
    if (mod && mod.__esModule) return mod;
    var result = {};
    if (mod != null) for (var k in mod) if (k !== "default" && Object.prototype.hasOwnProperty.call(mod, k)) __createBinding(result, mod, k);
    __setModuleDefault(result, mod);
    return result;
};
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.FileContentManager = exports.FilePathManager = exports.ProjectWhiteListManager = exports.clearProjectWhiteListManager = exports.initProjectWhiteListManager = exports.projectWhiteListManager = exports.DECORATOR_WHITE_LIST = exports.PROJECT_WHITE_LIST = exports.FILE_WHITE_LISTS = exports.FILE_NAMES_MAP = exports.TRANSFORMED_PATH = exports.SOURCE_FILE_PATHS = exports.areSetsEqual = exports.setToArray = exports.arrayToSet = exports.addToSet = void 0;
const ApiExtractor_1 = require("../common/ApiExtractor");
const FileUtils_1 = require("./FileUtils");
const CommonCollections_1 = require("./CommonCollections");
const crypto = __importStar(require("crypto"));
const fs_1 = __importDefault(require("fs"));
const path_1 = __importDefault(require("path"));
function addToSet(targetSet, sourceSet) {
    sourceSet.forEach((element) => targetSet.add(element));
}
exports.addToSet = addToSet;
function arrayToSet(array) {
    return new Set(array);
}
exports.arrayToSet = arrayToSet;
function setToArray(set) {
    return Array.from(set);
}
exports.setToArray = setToArray;
function areSetsEqual(setA, setB) {
    if (setA.size !== setB.size) {
        return false;
    }
    for (const setItem of setA) {
        if (!setB.has(setItem)) {
            return false;
        }
    }
    return true;
}
exports.areSetsEqual = areSetsEqual;
exports.SOURCE_FILE_PATHS = 'sourceFilePaths.cache';
exports.TRANSFORMED_PATH = 'transformed';
exports.FILE_NAMES_MAP = 'transformedFileNamesMap.json';
exports.FILE_WHITE_LISTS = 'fileWhiteLists.json';
exports.PROJECT_WHITE_LIST = 'projectWhiteList.json';
// this while list is only used for bytecode obfuscation
exports.DECORATOR_WHITE_LIST = [
    'Monitor',
    'Track',
    'Trace',
    'AnimatableExtend'
];
// Initialize projectWhiteListManager
function initProjectWhiteListManager(cachePath, isIncremental, enableAtKeep) {
    exports.projectWhiteListManager = new ProjectWhiteListManager(cachePath, isIncremental, enableAtKeep);
}
exports.initProjectWhiteListManager = initProjectWhiteListManager;
// Clear projectWhiteListManager
function clearProjectWhiteListManager() {
    exports.projectWhiteListManager = undefined;
}
exports.clearProjectWhiteListManager = clearProjectWhiteListManager;
/**
 * This class is used to manage project white lists.
 * Used to collect white list of each file and merge them into project white lists.
 */
class ProjectWhiteListManager {
    getEnableAtKeep() {
        return this.enableAtKeep;
    }
    getFileWhiteListsCachePath() {
        return this.fileWhiteListsCachePath;
    }
    getProjectWhiteListCachePath() {
        return this.projectWhiteListCachePath;
    }
    getIsIncremental() {
        return this.isIncremental;
    }
    getShouldReObfuscate() {
        return this.shouldReObfuscate;
    }
    getFileWhiteListMap() {
        return this.fileWhiteListMap;
    }
    constructor(cachePath, isIncremental, enableAtKeep) {
        // If it is incremental compliation
        this.isIncremental = false;
        // If we should reObfuscate all files
        this.shouldReObfuscate = false;
        this.fileWhiteListsCachePath = path_1.default.join(cachePath, exports.FILE_WHITE_LISTS);
        this.projectWhiteListCachePath = path_1.default.join(cachePath, exports.PROJECT_WHITE_LIST);
        this.isIncremental = isIncremental;
        this.enableAtKeep = enableAtKeep;
        this.fileWhiteListMap = new Map();
    }
    createDefaultFileKeepInfo() {
        return {
            structProperties: new Set(),
            exported: {
                propertyNames: new Set(),
                globalNames: new Set(),
            },
            enumProperties: new Set(),
            stringProperties: new Set(),
            arkUIKeepInfo: {
                propertyNames: new Set(),
                globalNames: new Set(),
            },
        };
    }
    createDefaultFileReservedInfo() {
        return {
            enumProperties: new Set(),
            propertyParams: new Set(),
        };
    }
    // Create one fileWhilteList object, with keepSymbol & keepAsConsumer if atKeep is enabled
    createFileWhiteList() {
        const fileKeepInfo = this.enableAtKeep
            ? Object.assign({ keepSymbol: {
                    propertyNames: new Set(),
                    globalNames: new Set(),
                }, keepAsConsumer: {
                    propertyNames: new Set(),
                    globalNames: new Set(),
                } }, this.createDefaultFileKeepInfo()) : this.createDefaultFileKeepInfo();
        return {
            fileKeepInfo,
            fileReservedInfo: this.createDefaultFileReservedInfo(),
        };
    }
    // Initialize current collector,
    // should be called before we collect fileWhiteLists.
    setCurrentCollector(path) {
        const unixPath = FileUtils_1.FileUtils.toUnixPath(path);
        let fileWhiteListInfo = this.fileWhiteListMap.get(unixPath);
        if (!fileWhiteListInfo) {
            fileWhiteListInfo = this.createFileWhiteList();
            this.fileWhiteListMap.set(unixPath, fileWhiteListInfo);
        }
        this.fileWhiteListInfo = fileWhiteListInfo;
    }
    readFileWhiteLists(filePath) {
        var _a;
        const fileContent = fs_1.default.readFileSync(filePath, 'utf8');
        const parsed = JSON.parse(fileContent);
        const map = new Map();
        for (const key in parsed) {
            if (Object.prototype.hasOwnProperty.call(parsed, key)) {
                const fileKeepInfo = {
                    keepSymbol: parsed[key].fileKeepInfo.keepSymbol
                        ? {
                            propertyNames: arrayToSet(parsed[key].fileKeepInfo.keepSymbol.propertyNames),
                            globalNames: arrayToSet(parsed[key].fileKeepInfo.keepSymbol.globalNames),
                        }
                        : undefined,
                    keepAsConsumer: parsed[key].fileKeepInfo.keepAsConsumer
                        ? {
                            propertyNames: arrayToSet(parsed[key].fileKeepInfo.keepAsConsumer.propertyNames),
                            globalNames: arrayToSet(parsed[key].fileKeepInfo.keepAsConsumer.globalNames),
                        }
                        : undefined,
                    structProperties: arrayToSet(parsed[key].fileKeepInfo.structProperties),
                    exported: {
                        propertyNames: arrayToSet(parsed[key].fileKeepInfo.exported.propertyNames),
                        globalNames: arrayToSet(parsed[key].fileKeepInfo.exported.globalNames),
                    },
                    enumProperties: arrayToSet(parsed[key].fileKeepInfo.enumProperties),
                    stringProperties: arrayToSet(parsed[key].fileKeepInfo.stringProperties),
                    arkUIKeepInfo: parsed[key].fileKeepInfo.arkUIKeepInfo
                        ? {
                            propertyNames: arrayToSet(parsed[key].fileKeepInfo.arkUIKeepInfo.propertyNames),
                            globalNames: arrayToSet(parsed[key].fileKeepInfo.arkUIKeepInfo.globalNames),
                        }
                        : undefined,
                };
                const fileReservedInfo = {
                    enumProperties: arrayToSet(parsed[key].fileReservedInfo.enumProperties),
                    propertyParams: arrayToSet(parsed[key].fileReservedInfo.propertyParams),
                };
                const bytecodeObfuscateKeepInfo = {
                    decoratorMap: (_a = parsed[key].bytecodeObfuscateKeepInfo) === null || _a === void 0 ? void 0 : _a.decoratorMap,
                };
                map.set(key, { fileKeepInfo, fileReservedInfo, bytecodeObfuscateKeepInfo });
            }
        }
        return map;
    }
    writeFileWhiteLists(filePath, fileWhiteLists) {
        var _a;
        const jsonData = {};
        for (const [key, value] of fileWhiteLists) {
            jsonData[key] = {
                fileKeepInfo: {
                    keepSymbol: value.fileKeepInfo.keepSymbol
                        ? {
                            propertyNames: setToArray(value.fileKeepInfo.keepSymbol.propertyNames),
                            globalNames: setToArray(value.fileKeepInfo.keepSymbol.globalNames),
                        }
                        : undefined,
                    keepAsConsumer: value.fileKeepInfo.keepAsConsumer
                        ? {
                            propertyNames: setToArray(value.fileKeepInfo.keepAsConsumer.propertyNames),
                            globalNames: setToArray(value.fileKeepInfo.keepAsConsumer.globalNames),
                        }
                        : undefined,
                    structProperties: setToArray(value.fileKeepInfo.structProperties),
                    exported: {
                        propertyNames: setToArray(value.fileKeepInfo.exported.propertyNames),
                        globalNames: setToArray(value.fileKeepInfo.exported.globalNames),
                    },
                    enumProperties: setToArray(value.fileKeepInfo.enumProperties),
                    stringProperties: setToArray(value.fileKeepInfo.stringProperties),
                    arkUIKeepInfo: value.fileKeepInfo.arkUIKeepInfo
                        ? {
                            propertyNames: setToArray(value.fileKeepInfo.arkUIKeepInfo.propertyNames),
                            globalNames: setToArray(value.fileKeepInfo.arkUIKeepInfo.globalNames),
                        }
                        : undefined,
                },
                fileReservedInfo: {
                    enumProperties: setToArray(value.fileReservedInfo.enumProperties),
                    propertyParams: setToArray(value.fileReservedInfo.propertyParams),
                },
            };
            if ((_a = value.bytecodeObfuscateKeepInfo) === null || _a === void 0 ? void 0 : _a.decoratorMap) {
                jsonData[key].bytecodeObfuscateKeepInfo = {
                    decoratorMap: value.bytecodeObfuscateKeepInfo.decoratorMap,
                };
            }
        }
        const jsonString = JSON.stringify(jsonData, null, 2);
        fs_1.default.writeFileSync(filePath, jsonString, 'utf8');
    }
    readProjectWhiteList(filePath) {
        const fileContent = fs_1.default.readFileSync(filePath, 'utf8');
        const parsed = JSON.parse(fileContent);
        const projectKeepInfo = {
            propertyNames: arrayToSet(parsed.projectKeepInfo.propertyNames),
            globalNames: arrayToSet(parsed.projectKeepInfo.globalNames),
        };
        const projectReservedInfo = {
            enumProperties: arrayToSet(parsed.projectReservedInfo.enumProperties),
            propertyParams: arrayToSet(parsed.projectReservedInfo.propertyParams),
        };
        return {
            projectKeepInfo,
            projectReservedInfo,
        };
    }
    writeProjectWhiteList(filePath, projectWhiteList) {
        const jsonData = {
            projectKeepInfo: {
                propertyNames: setToArray(projectWhiteList.projectKeepInfo.propertyNames),
                globalNames: setToArray(projectWhiteList.projectKeepInfo.globalNames),
            },
            projectReservedInfo: {
                enumProperties: setToArray(projectWhiteList.projectReservedInfo.enumProperties),
                propertyParams: setToArray(projectWhiteList.projectReservedInfo.propertyParams),
            },
        };
        const jsonString = JSON.stringify(jsonData, null, 2);
        fs_1.default.writeFileSync(filePath, jsonString, 'utf8');
    }
    updateFileWhiteListMap(deletedFilePath) {
        const lastFileWhiteLists = this.readFileWhiteLists(this.fileWhiteListsCachePath);
        deletedFilePath === null || deletedFilePath === void 0 ? void 0 : deletedFilePath.forEach((path) => {
            lastFileWhiteLists.delete(path);
        });
        this.fileWhiteListMap.forEach((value, key) => {
            lastFileWhiteLists.set(key, value);
        });
        this.writeFileWhiteLists(this.fileWhiteListsCachePath, lastFileWhiteLists);
        this.fileWhiteListMap = lastFileWhiteLists;
    }
    createProjectWhiteList(fileWhiteLists) {
        const projectKeepInfo = {
            propertyNames: new Set(),
            globalNames: new Set(),
        };
        const projectReservedInfo = {
            enumProperties: new Set(),
            propertyParams: new Set(),
        };
        fileWhiteLists.forEach((fileWhiteList) => {
            var _a, _b, _c, _d, _e, _f, _g, _h;
            // 1. Collect fileKeepInfo
            // Collect keepSymbol
            (_a = fileWhiteList.fileKeepInfo.keepSymbol) === null || _a === void 0 ? void 0 : _a.globalNames.forEach((globalName) => {
                projectKeepInfo.globalNames.add(globalName);
            });
            (_b = fileWhiteList.fileKeepInfo.keepSymbol) === null || _b === void 0 ? void 0 : _b.propertyNames.forEach((propertyName) => {
                projectKeepInfo.propertyNames.add(propertyName);
            });
            // Collect keepAsConsumer
            (_c = fileWhiteList.fileKeepInfo.keepAsConsumer) === null || _c === void 0 ? void 0 : _c.globalNames.forEach((globalName) => {
                projectKeepInfo.globalNames.add(globalName);
            });
            (_d = fileWhiteList.fileKeepInfo.keepAsConsumer) === null || _d === void 0 ? void 0 : _d.propertyNames.forEach((propertyName) => {
                projectKeepInfo.propertyNames.add(propertyName);
            });
            // Collect structProperties
            fileWhiteList.fileKeepInfo.structProperties.forEach((propertyName) => {
                projectKeepInfo.propertyNames.add(propertyName);
            });
            // Collect exportedNames
            fileWhiteList.fileKeepInfo.exported.globalNames.forEach((globalName) => {
                projectKeepInfo.globalNames.add(globalName);
            });
            fileWhiteList.fileKeepInfo.exported.propertyNames.forEach((propertyName) => {
                projectKeepInfo.propertyNames.add(propertyName);
            });
            // Collect enumProperties
            fileWhiteList.fileKeepInfo.enumProperties.forEach((propertyName) => {
                projectKeepInfo.propertyNames.add(propertyName);
            });
            // Collect stringProperties
            fileWhiteList.fileKeepInfo.stringProperties.forEach((propertyName) => {
                projectKeepInfo.propertyNames.add(propertyName);
            });
            // Collect arkUIKeepInfo
            (_e = fileWhiteList.fileKeepInfo.arkUIKeepInfo) === null || _e === void 0 ? void 0 : _e.globalNames.forEach((globalName) => {
                projectKeepInfo.globalNames.add(globalName);
                CommonCollections_1.AtIntentCollections.globalNames.add(globalName);
            });
            (_f = fileWhiteList.fileKeepInfo.arkUIKeepInfo) === null || _f === void 0 ? void 0 : _f.propertyNames.forEach((propertyName) => {
                projectKeepInfo.propertyNames.add(propertyName);
                CommonCollections_1.AtIntentCollections.propertyNames.add(propertyName);
            });
            // 2. Collect fileReservedInfo
            // Collect enumProperties
            fileWhiteList.fileReservedInfo.enumProperties.forEach((enumPropertyName) => {
                projectReservedInfo.enumProperties.add(enumPropertyName);
            });
            // Collect propertyParams
            fileWhiteList.fileReservedInfo.propertyParams.forEach((propertyParam) => {
                projectReservedInfo.propertyParams.add(propertyParam);
            });
            const decoratorMap = (_g = fileWhiteList.bytecodeObfuscateKeepInfo) === null || _g === void 0 ? void 0 : _g.decoratorMap;
            for (const key in decoratorMap) {
                if (Object.prototype.hasOwnProperty.call(decoratorMap, key)) {
                    (_h = decoratorMap[key]) === null || _h === void 0 ? void 0 : _h.forEach(item => projectKeepInfo.globalNames.add(item));
                }
            }
        });
        const projectWhiteList = {
            projectKeepInfo: projectKeepInfo,
            projectReservedInfo: projectReservedInfo,
        };
        return projectWhiteList;
    }
    // Determine if the project's white list has been updated.
    areProjectWhiteListsEqual(whiteList1, whiteList2) {
        const projectKeepInfoEqual = areSetsEqual(whiteList1.projectKeepInfo.propertyNames, whiteList2.projectKeepInfo.propertyNames) &&
            areSetsEqual(whiteList1.projectKeepInfo.globalNames, whiteList2.projectKeepInfo.globalNames);
        if (!projectKeepInfoEqual) {
            return false;
        }
        const projectReservedInfoEqual = areSetsEqual(whiteList1.projectReservedInfo.enumProperties, whiteList2.projectReservedInfo.enumProperties) &&
            areSetsEqual(whiteList1.projectReservedInfo.propertyParams, whiteList2.projectReservedInfo.propertyParams);
        return projectReservedInfoEqual;
    }
    // We only scan updated files or newly created files in incremental compliation,
    // so we need to update the UnobfuscationCollections and reserved names use all file white lists.
    // This should be called in incremental compliation after we updated file white list.
    updateUnobfuscationCollections() {
        this.fileWhiteListMap.forEach((fileWhiteList) => {
            var _a, _b;
            if (this.enableAtKeep) {
                addToSet(CommonCollections_1.AtKeepCollections.keepSymbol.propertyNames, fileWhiteList.fileKeepInfo.keepSymbol.propertyNames);
                addToSet(CommonCollections_1.AtKeepCollections.keepSymbol.globalNames, fileWhiteList.fileKeepInfo.keepSymbol.globalNames);
                addToSet(CommonCollections_1.AtKeepCollections.keepAsConsumer.propertyNames, fileWhiteList.fileKeepInfo.keepAsConsumer.propertyNames);
                addToSet(CommonCollections_1.AtKeepCollections.keepAsConsumer.globalNames, fileWhiteList.fileKeepInfo.keepAsConsumer.globalNames);
            }
            addToSet(CommonCollections_1.UnobfuscationCollections.reservedStruct, fileWhiteList.fileKeepInfo.structProperties);
            addToSet(CommonCollections_1.UnobfuscationCollections.reservedEnum, fileWhiteList.fileKeepInfo.enumProperties);
            addToSet(CommonCollections_1.UnobfuscationCollections.reservedExportName, fileWhiteList.fileKeepInfo.exported.globalNames);
            addToSet(CommonCollections_1.UnobfuscationCollections.reservedExportNameAndProp, fileWhiteList.fileKeepInfo.exported.propertyNames);
            addToSet(CommonCollections_1.UnobfuscationCollections.reservedStrProp, fileWhiteList.fileKeepInfo.stringProperties);
            if (fileWhiteList.fileKeepInfo.arkUIKeepInfo) {
                addToSet(CommonCollections_1.AtIntentCollections.propertyNames, fileWhiteList.fileKeepInfo.arkUIKeepInfo.propertyNames);
                addToSet(CommonCollections_1.AtIntentCollections.globalNames, fileWhiteList.fileKeepInfo.arkUIKeepInfo.globalNames);
            }
            addToSet(ApiExtractor_1.ApiExtractor.mConstructorPropertySet, fileWhiteList.fileReservedInfo.propertyParams);
            addToSet(ApiExtractor_1.ApiExtractor.mEnumMemberSet, fileWhiteList.fileReservedInfo.enumProperties);
            const decoratorMap = (_a = fileWhiteList.bytecodeObfuscateKeepInfo) === null || _a === void 0 ? void 0 : _a.decoratorMap;
            for (const key in decoratorMap) {
                if (Object.prototype.hasOwnProperty.call(decoratorMap, key)) {
                    (_b = decoratorMap[key]) === null || _b === void 0 ? void 0 : _b.forEach(item => CommonCollections_1.BytecodeObfuscationCollections.decoratorProp.add(item));
                }
            }
        });
    }
    updateProjectWhiteList() {
        const lastProjectWhiteList = this.readProjectWhiteList(this.projectWhiteListCachePath);
        const newestProjectWhiteList = this.createProjectWhiteList(this.fileWhiteListMap);
        if (!this.areProjectWhiteListsEqual(lastProjectWhiteList, newestProjectWhiteList)) {
            this.writeProjectWhiteList(this.projectWhiteListCachePath, newestProjectWhiteList);
            this.shouldReObfuscate = true;
        }
        return newestProjectWhiteList;
    }
    createOrUpdateWhiteListCaches(deletedFilePath) {
        let projectWhiteList;
        if (!this.isIncremental) {
            this.writeFileWhiteLists(this.fileWhiteListsCachePath, this.fileWhiteListMap);
            projectWhiteList = this.createProjectWhiteList(this.fileWhiteListMap);
            this.writeProjectWhiteList(this.projectWhiteListCachePath, projectWhiteList);
        }
        else {
            this.updateFileWhiteListMap(deletedFilePath);
            projectWhiteList = this.updateProjectWhiteList();
            this.updateUnobfuscationCollections();
        }
        this.fileWhiteListMap.clear();
    }
}
exports.ProjectWhiteListManager = ProjectWhiteListManager;
/**
 * This class is used to manage sourceFilePath.json file.
 * Will be created when initialize arkObfuscator.
 */
class FilePathManager {
    getSourceFilePaths() {
        return this.sourceFilePaths;
    }
    getDeletedSourceFilePaths() {
        return this.deletedSourceFilePaths;
    }
    getAddedSourceFilePaths() {
        return this.addedSourceFilePaths;
    }
    getFilePathsCache() {
        return this.filePathsCache;
    }
    constructor(cachePath) {
        this.filePathsCache = path_1.default.join(cachePath, exports.SOURCE_FILE_PATHS);
        this.sourceFilePaths = new Set();
        this.deletedSourceFilePaths = new Set();
        this.addedSourceFilePaths = new Set();
    }
    setSourceFilePaths(sourceFilePaths) {
        sourceFilePaths.forEach((filePath) => {
            if (!FileUtils_1.FileUtils.isReadableFile(filePath) || !ApiExtractor_1.ApiExtractor.isParsableFile(filePath)) {
                return;
            }
            this.sourceFilePaths.add(filePath);
        });
    }
    writeSourceFilePaths() {
        const content = Array.from(this.sourceFilePaths).join('\n');
        FileUtils_1.FileUtils.writeFile(this.filePathsCache, content);
    }
    // Update sourceFilePaths.cache and get deleted file list and added file list
    updateSourceFilePaths() {
        const cacheContent = FileUtils_1.FileUtils.readFile(this.filePathsCache).split('\n');
        const cacheSet = new Set(cacheContent);
        for (const path of cacheSet) {
            if (!this.sourceFilePaths.has(path)) {
                this.deletedSourceFilePaths.add(path);
            }
        }
        for (const path of this.sourceFilePaths) {
            if (!cacheSet.has(path)) {
                this.addedSourceFilePaths.add(path);
            }
        }
        this.writeSourceFilePaths();
    }
    // Create or update sourceFilePaths.cache
    createOrUpdateSourceFilePaths(sourceFilePaths) {
        this.setSourceFilePaths(sourceFilePaths);
        if (this.isIncremental()) {
            this.updateSourceFilePaths();
        }
        else {
            this.writeSourceFilePaths();
        }
    }
    isIncremental() {
        return fs_1.default.existsSync(this.filePathsCache);
    }
}
exports.FilePathManager = FilePathManager;
/**
 * This class is used to manage transfromed file content.
 * Will be created when initialize arkObfuscator.
 */
class FileContentManager {
    getTransformedFilesDir() {
        return this.transformedFilesDir;
    }
    getFileNamesMapPath() {
        return this.fileNamesMapPath;
    }
    getIsIncremental() {
        return this.isIncremental;
    }
    constructor(cachePath, isIncremental) {
        this.transformedFilesDir = path_1.default.join(cachePath, exports.TRANSFORMED_PATH);
        this.fileNamesMapPath = path_1.default.join(this.transformedFilesDir, exports.FILE_NAMES_MAP);
        this.fileNamesMap = new Map();
        this.isIncremental = isIncremental;
        FileUtils_1.FileUtils.createDirectory(this.transformedFilesDir);
    }
    // Generate hash from filePah
    generatePathHash(filePath) {
        const hash = crypto.createHash('md5');
        hash.update(filePath);
        return hash.digest('hex').slice(0, 16);
    }
    // Generate new file name for transfromed sourcefiles.
    // Generated from origin filePath's hash & time stamp.
    generateFileName(filePath) {
        const hash = this.generatePathHash(filePath);
        const timestamp = Date.now().toString();
        return `${hash}_${timestamp}`;
    }
    // Delete a set of files, used in incremental compliation if we have deleted some files
    deleteFileContent(deletedSourceFilePaths) {
        deletedSourceFilePaths.forEach((filePath) => {
            this.deleteTransformedFile(filePath);
        });
    }
    // Delete transfromed file
    deleteTransformedFile(deletedSourceFilePath) {
        if (this.fileNamesMap.has(deletedSourceFilePath)) {
            const transformedFilePath = this.fileNamesMap.get(deletedSourceFilePath);
            this.fileNamesMap.delete(deletedSourceFilePath);
            FileUtils_1.FileUtils.deleteFile(transformedFilePath);
        }
    }
    readFileNamesMap() {
        const jsonObject = FileUtils_1.FileUtils.readFileAsJson(this.fileNamesMapPath);
        this.fileNamesMap = new Map(Object.entries(jsonObject));
    }
    writeFileNamesMap() {
        const jsonObject = Object.fromEntries(this.fileNamesMap.entries());
        const jsonString = JSON.stringify(jsonObject, null, 2);
        FileUtils_1.FileUtils.writeFile(this.fileNamesMapPath, jsonString);
        this.fileNamesMap.clear();
    }
    readFileContent(transformedFilePath) {
        const fileContentJson = FileUtils_1.FileUtils.readFile(transformedFilePath);
        const fileConstent = JSON.parse(fileContentJson);
        return fileConstent;
    }
    writeFileContent(filePath, fileContent) {
        const jsonString = JSON.stringify(fileContent, null, 2);
        FileUtils_1.FileUtils.writeFile(filePath, jsonString);
    }
    // Update file content for newly created files or updated files
    updateFileContent(fileContent) {
        const originPath = FileUtils_1.FileUtils.toUnixPath(fileContent.moduleInfo.originSourceFilePath);
        if (this.isIncremental) {
            this.deleteTransformedFile(originPath);
        }
        const fileName = this.generateFileName(originPath);
        const transformedFilePath = path_1.default.join(this.transformedFilesDir, fileName);
        this.fileNamesMap.set(originPath, transformedFilePath);
        this.writeFileContent(transformedFilePath, fileContent);
    }
    getSortedFiles() {
        return (this.fileNamesMap && Array.from(this.fileNamesMap.keys())).sort((a, b) => a.localeCompare(b)) || [];
    }
}
exports.FileContentManager = FileContentManager;
//# sourceMappingURL=ProjectCollections.js.map
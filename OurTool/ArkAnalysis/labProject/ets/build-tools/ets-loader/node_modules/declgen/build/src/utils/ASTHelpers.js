"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.visitVisitResult = exports.isVisitResultNodeArray = exports.isVisitResultNode = void 0;
function isVisitResultNode(vr) {
    return vr !== undefined && !Array.isArray(vr);
}
exports.isVisitResultNode = isVisitResultNode;
function isVisitResultNodeArray(vr) {
    return Array.isArray(vr);
}
exports.isVisitResultNodeArray = isVisitResultNodeArray;
function visitVisitResult(result, visitor) {
    if (result === undefined) {
        return undefined;
    }
    if (isVisitResultNode(result)) {
        return visitor(result);
    }
    if (!isVisitResultNodeArray(result)) {
        throw new Error('Unreachable');
    }
    const newResult = [];
    for (const node of result) {
        const fixedNode = visitor(node);
        if (fixedNode === undefined) {
            continue;
        }
        if (Array.isArray(fixedNode)) {
            newResult.push(...fixedNode);
            continue;
        }
        newResult.push(fixedNode);
    }
    if (newResult.length === 0) {
        return undefined;
    }
    if (newResult.length === 1) {
        return newResult[0];
    }
    return newResult;
}
exports.visitVisitResult = visitVisitResult;
//# sourceMappingURL=ASTHelpers.js.map
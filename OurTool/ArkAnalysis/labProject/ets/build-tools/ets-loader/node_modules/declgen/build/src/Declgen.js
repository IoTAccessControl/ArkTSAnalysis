"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.Declgen = void 0;
const fs = require("fs");
const ts = require("typescript");
const path = require("path");
const Compiler_1 = require("./compiler/Compiler");
const Logger_1 = require("../utils/logger/Logger");
const ASTTransformer_1 = require("./ASTTransformer");
const ASTAutofixer_1 = require("./ASTAutofixer");
class Declgen {
    constructor(declgenOptions, customResolveModuleNames, compilerOptions) {
        const { rootNames, options } = Declgen.parseDeclgenOptions(declgenOptions);
        this.rootFiles = rootNames;
        this.declgenOptions = declgenOptions;
        this.sourceFileMap = new Map();
        this.compilerOptions = Object.assign({}, options, Object.assign(Object.assign({ declaration: true, emitDeclarationOnly: true, outDir: declgenOptions.outDir }, (declgenOptions.rootDir ? { rootDir: declgenOptions.rootDir } : {})), (compilerOptions !== null && compilerOptions !== void 0 ? compilerOptions : {})));
        this.compilerOptions.noEmit = false;
        this.hookedHost = Declgen.createHookedCompilerHost(this.sourceFileMap, this.compilerOptions, declgenOptions);
        if (customResolveModuleNames) {
            this.hookedHost.resolveModuleNames = customResolveModuleNames;
        }
    }
    run() {
        let program = this.recompile();
        this.processDeclarationFiles(program);
        const emitResult = program.emit(undefined, undefined, undefined, true, {
            before: [],
            after: [],
            afterDeclarations: [
                (ctx) => {
                    const typeChecker = program.getTypeChecker();
                    const autofixer = new ASTAutofixer_1.Autofixer(typeChecker, ctx);
                    const transformer = new ASTTransformer_1.Transformer(ctx, this.sourceFileMap, [autofixer.fixNode.bind(autofixer)]);
                    return transformer.createCustomTransformer();
                }
            ]
        });
        return {
            emitResult: emitResult
        };
    }
    recompile() {
        return (0, Compiler_1.compile)(this.rootFiles, this.compilerOptions, this.hookedHost);
    }
    processDeclarationFiles(program) {
        const typeChecker = program.getTypeChecker();
        this.rootFiles.forEach((fileName) => {
            const sourceFile = program.getSourceFile(fileName);
            if (sourceFile && sourceFile.isDeclarationFile) {
                const compilerOptions = program.getCompilerOptions();
                const outDir = compilerOptions.outDir || path.dirname(fileName);
                const rootDir = compilerOptions.rootDir || path.dirname(fileName);
                const relativePath = path.relative(rootDir, fileName);
                const fileNameWithoutExt = relativePath.replace(/\.d\.(ts|ets)$/, '');
                const dEtsFilePath = path.join(outDir, `${fileNameWithoutExt}${".d.ets"}`);
                const result = this.transformDeclarationFiles(program, sourceFile, typeChecker);
                const printer = ts.createPrinter();
                const transformedCode = printer.printFile(result.transformed[0]);
                if (Declgen.isFileInAllowedPath(this.declgenOptions, [sourceFile])) {
                    if (!fs.existsSync(outDir)) {
                        fs.mkdirSync(outDir, { recursive: true });
                    }
                    fs.writeFileSync(dEtsFilePath, transformedCode, { encoding: 'utf8' });
                }
            }
        });
    }
    transformDeclarationFiles(program, sourceFile, typeChecker) {
        const result = ts.transform(sourceFile, [
            (context) => {
                const autofixer = new ASTAutofixer_1.Autofixer(typeChecker, context);
                const visit = (node) => {
                    const fixedNode = autofixer.fixNode(node);
                    if (fixedNode === undefined) {
                        return node;
                    }
                    else if (Array.isArray(fixedNode)) {
                        return context.factory.createBlock(fixedNode);
                    }
                    else {
                        return ts.visitEachChild(fixedNode, visit, context);
                    }
                };
                return (sourceFile) => {
                    return visit(sourceFile);
                };
            }
        ], program.getCompilerOptions());
        return result;
    }
    static createHookedCompilerHost(sourceFileMap, compilerOptions, declgenOptions) {
        const host = ts.createCompilerHost(compilerOptions);
        const fallbackGetSourceFile = host.getSourceFile;
        const fallbackWriteFile = host.writeFile;
        return Object.assign(host, {
            getSourceFile(fileName, languageVersionOrOptions, onError, shouldCreateNewSourceFile) {
                var _a;
                return ((_a = sourceFileMap.get(fileName)) !== null && _a !== void 0 ? _a : fallbackGetSourceFile(fileName, languageVersionOrOptions, onError, shouldCreateNewSourceFile));
            },
            writeFile(fileName, text, writeByteOrderMark, onError, sourceFiles, data) {
                if (!Declgen.isFileInAllowedPath(declgenOptions, sourceFiles)) {
                    return;
                }
                const parsedPath = path.parse(fileName);
                fallbackWriteFile(path.join(parsedPath.dir, `${parsedPath.name}${".ets"}`), text, writeByteOrderMark, onError, sourceFiles, data);
            }
        });
    }
    static isFileInAllowedPath(declgenOptions, sourceFiles) {
        var _a;
        if (!((_a = declgenOptions.includePaths) === null || _a === void 0 ? void 0 : _a.length)) {
            return true;
        }
        if (!(sourceFiles === null || sourceFiles === void 0 ? void 0 : sourceFiles.length)) {
            return false;
        }
        const filePath = path.resolve(sourceFiles[0].fileName);
        return declgenOptions.includePaths.some((allowedPath) => {
            return filePath.startsWith(path.resolve(allowedPath));
        });
    }
    static parseDeclgenOptions(opts) {
        const parsedConfigFile = opts.tsconfig ? (0, Compiler_1.parseConfigFile)(opts.tsconfig) : undefined;
        if (parsedConfigFile) {
            return {
                rootNames: parsedConfigFile.fileNames,
                options: parsedConfigFile.options,
                projectReferences: parsedConfigFile.projectReferences,
                configFileParsingDiagnostics: ts.getConfigFileParsingDiagnostics(parsedConfigFile)
            };
        }
        return {
            rootNames: Declgen.collectInputFiles(opts),
            options: (0, Compiler_1.defaultCompilerOptions)()
        };
    }
    static collectInputFiles(opts) {
        const inputFiles = [];
        if (opts.inputFiles) {
            inputFiles.push(...opts.inputFiles);
        }
        if (opts.inputDirs) {
            for (const dir of opts.inputDirs) {
                try {
                    inputFiles.push(...(0, Compiler_1.getSourceFilesFromDir)(dir));
                }
                catch (error) {
                    Logger_1.Logger.error('Failed to read folder: ' + error);
                    process.exit(-1);
                }
            }
        }
        return inputFiles;
    }
}
exports.Declgen = Declgen;
//# sourceMappingURL=Declgen.js.map
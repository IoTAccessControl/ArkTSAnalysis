"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.getSourceFilesFromDir = exports.parseConfigFile = exports.compile = exports.defaultCompilerOptions = void 0;
const ts = require("typescript");
const fs = require("node:fs");
const path = require("node:path");
const LogTscDiagnostics_1 = require("../utils/LogTscDiagnostics");
const Logger_1 = require("../../utils/logger/Logger");
function defaultCompilerOptions() {
    return {
        target: ts.ScriptTarget.Latest,
        module: ts.ModuleKind.CommonJS,
        allowJs: true,
        checkJs: true
    };
}
exports.defaultCompilerOptions = defaultCompilerOptions;
function compile(rootFileNames, compilerOptions, compilerHost) {
    const program = ts.createProgram(rootFileNames, compilerOptions !== null && compilerOptions !== void 0 ? compilerOptions : defaultCompilerOptions(), compilerHost);
    const diagnostics = ts.getPreEmitDiagnostics(program);
    (0, LogTscDiagnostics_1.logTscDiagnostics)(diagnostics, Logger_1.LogLevel.ERROR);
    return program;
}
exports.compile = compile;
function parseConfigFile(tsconfig) {
    const host = ts.sys;
    const diagnostics = [];
    let parsedConfigFile;
    try {
        const oldUnrecoverableDiagnostic = host.onUnRecoverableConfigFileDiagnostic;
        host.onUnRecoverableConfigFileDiagnostic = (diagnostic) => {
            diagnostics.push(diagnostic);
        };
        parsedConfigFile = ts.getParsedCommandLineOfConfigFile(tsconfig, {}, host);
        host.onUnRecoverableConfigFileDiagnostic = oldUnrecoverableDiagnostic;
        if (parsedConfigFile) {
            diagnostics.push(...ts.getConfigFileParsingDiagnostics(parsedConfigFile));
        }
        if (diagnostics.length > 0) {
            Logger_1.Logger.error('Failed to read config file.');
            (0, LogTscDiagnostics_1.logTscDiagnostics)(diagnostics, Logger_1.LogLevel.ERROR);
            process.exit(-1);
        }
    }
    catch (error) {
        Logger_1.Logger.error('Failed to read config file: ' + error);
        process.exit(-1);
    }
    return parsedConfigFile;
}
exports.parseConfigFile = parseConfigFile;
function getSourceFilesFromDir(dir) {
    const resultFiles = [];
    for (const entity of fs.readdirSync(dir)) {
        const entityName = path.join(dir, entity);
        if (fs.statSync(entityName).isFile()) {
            const extension = path.extname(entityName);
            if (extension === ts.Extension.Ts || extension === ts.Extension.Tsx) {
                resultFiles.push(entityName);
            }
        }
        if (fs.statSync(entityName).isDirectory()) {
            resultFiles.push(...getSourceFilesFromDir(entityName));
            continue;
        }
    }
    return resultFiles;
}
exports.getSourceFilesFromDir = getSourceFilesFromDir;
//# sourceMappingURL=Compiler.js.map